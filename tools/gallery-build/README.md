# gallery-build tools

`gallery.json` / `thumbs` / `images_light` 生成系コマンドの入口です。

## Entrypoints

- `./export.sh`: `auto-post export-gallery-json`
- `./tag-recalc.sh`: `node apps/admin-web/scripts/tag-recalc.mjs`
